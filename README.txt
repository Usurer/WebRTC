To run websocket-server under node.js, install 'websocket' module:
npm install websocket
from the folder where websocket-server.js is located.
Run then:
node websocket-server.js 

second.html is a Slave
second_active.html is Master

It doesn't work good yet, so read comments in second.js script.